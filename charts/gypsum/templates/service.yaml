apiVersion: v1
kind: Service
metadata:
  name: {{ include "gypsum.fullname" . }}
  labels:
    {{- include "gypsum.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    {{- if .Values.metrics.enabled }}
    - port: {{ .Values.metrics.port | default 9090 }}
      targetPort: metrics
      protocol: TCP
      name: metrics
    {{- end }}
  selector:
    {{- include "gypsum.selectorLabels" . | nindent 4 }}
